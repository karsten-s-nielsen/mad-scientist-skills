# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Python (skill scripts + their test suites)
__pycache__/
*.pyc

# Environment
.env
.env.*

# Claude Code local settings
.claude/settings.local.json

# Superpowers plugin working notes (brainstorming plans and specs). Session
# scratch, not project documentation — nothing under this path has ever been
# committed. Deliberately narrower than `docs/`: `docs/plans/` and `docs/adrs/`
# are tracked project history and must stay that way.
docs/superpowers/
